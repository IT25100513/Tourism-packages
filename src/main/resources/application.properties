spring.application.name=Tourism Package Customization Platform

server.port=8080

spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Relative to JVM working directory (project root when run from IntelliJ / mvn spring-boot:run)
app.data.directory=data
